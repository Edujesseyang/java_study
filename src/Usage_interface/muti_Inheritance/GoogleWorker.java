package src.Usage_interface.muti_Inheritance;

// a class can extends a father class as same time as implements an interface.
public class GoogleWorker extends Worker implements MaleProgrammer {

    // we don't have to override abstract methods from interface. because the father class have had already overridden.
    // this class inheritance is all overridden methods from its father.
}
